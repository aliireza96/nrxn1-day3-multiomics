## Processed assay inputs

This repository does not commit assay-level processed input files. Instead, it
documents the directory structure expected by the public analysis scripts.

Use this folder as the landing point for unpacked processed inputs before
running the workflow. See:

- `metadata/processed_input_manifest.tsv`
- the README file in each modality subdirectory
- the header comments in the corresponding analysis scripts
